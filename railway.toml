[build]
builder = "nixpacks"
buildCommand = "cd Tback && pip install -r requirements.txt && python manage.py collectstatic --noinput"

[deploy]
startCommand = "cd Tback && python manage.py migrate && gunicorn tback_api.wsgi --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10