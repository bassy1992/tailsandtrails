[phases.build]
cmds = ["cd Tback && pip install -r requirements.txt"]

[start]
cmd = "cd Tback && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn tback_api.wsgi --bind 0.0.0.0:$PORT"