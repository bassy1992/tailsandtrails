[phases.setup]
nixPkgs = ["python39", "pip"]

[phases.install]
cmds = ["cd Tback && pip install -r requirements.txt"]

[phases.build]
cmds = ["cd Tback && python manage.py collectstatic --noinput"]

[start]
cmd = "cd Tback && gunicorn tback_api.wsgi:application --bind 0.0.0.0:$PORT"