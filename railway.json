{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd Tback && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd Tback && python manage.py collectstatic --noinput && python manage.py migrate && gunicorn tback_api.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}