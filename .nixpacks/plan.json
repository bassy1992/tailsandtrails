{
  "providers": ["python"],
  "buildCmd": "cd Tback && pip install -r requirements.txt",
  "startCmd": "cd Tback && python manage.py collectstatic --noinput && python manage.py migrate && gunicorn tback_api.wsgi --bind 0.0.0.0:$PORT"
}